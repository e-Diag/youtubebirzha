# 🐳 Инструкция по запуску проекта в Docker

Подробная инструкция по сборке и запуску YouTube-Bot в Docker контейнерах.

## 📋 Требования

- **Docker Desktop** (Windows/Mac) или **Docker Engine + Docker Compose** (Linux)
- Минимум 2 ГБ свободного места на диске
- Минимум 2 ГБ оперативной памяти

### Установка Docker

**Windows/Mac:**
1. Скачайте [Docker Desktop](https://www.docker.com/products/docker-desktop/)
2. Установите и запустите приложение
3. Дождитесь полной загрузки (иконка Docker в трее должна быть зелёной)

**Linux (Ubuntu/Debian):**
```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt-get update
sudo apt-get install docker-compose-plugin

# Добавьте пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# Перезайдите в систему после этого
```

## 🚀 Быстрый старт

### Шаг 1: Подготовка проекта

Убедитесь, что вы находитесь в корневой директории проекта:
```bash
cd YouTube-Bot
```

### Шаг 2: Создание файла .env

Создайте файл `.env` в корне проекта с необходимыми переменными окружения.

**Windows PowerShell:**
```powershell
@"
DATABASE_URL=postgres://postgres:postgres@postgres:5432/youtube_market?sslmode=disable
PORT=8080
GIN_MODE=release
BOT_TOKEN=ваш_telegram_bot_token_здесь
MANAGER_ID=ваш_telegram_user_id_здесь
"@ | Out-File -FilePath .env -Encoding utf8
```

**Windows CMD:**
```cmd
echo DATABASE_URL=postgres://postgres:postgres@postgres:5432/youtube_market?sslmode=disable > .env
echo PORT=8080 >> .env
echo GIN_MODE=release >> .env
echo BOT_TOKEN=ваш_telegram_bot_token_здесь >> .env
echo MANAGER_ID=ваш_telegram_user_id_здесь >> .env
```

**Linux/Mac:**
```bash
cat > .env << EOF
DATABASE_URL=postgres://postgres:postgres@postgres:5432/youtube_market?sslmode=disable
PORT=8080
GIN_MODE=release
BOT_TOKEN=ваш_telegram_bot_token_здесь
MANAGER_ID=ваш_telegram_user_id_здесь
EOF
```

**Важно:** Замените `ваш_telegram_bot_token_здесь` и `ваш_telegram_user_id_здесь` на реальные значения:
- **BOT_TOKEN**: Получите у [@BotFather](https://t.me/BotFather) в Telegram
- **MANAGER_ID**: Ваш Telegram User ID (можно узнать у [@userinfobot](https://t.me/userinfobot))

### Шаг 3: Сборка и запуск

**Для новых версий Docker (20.10+):**
```bash
docker compose up -d --build
```

**Для старых версий Docker:**
```bash
docker-compose up -d --build
```

Флаг `--build` пересоберёт образы, если вы внесли изменения в код.

### Шаг 4: Проверка статуса

Проверьте, что все контейнеры запущены:
```bash
# Новая версия
docker compose ps

# Старая версия
docker-compose ps
```

Вы должны увидеть два контейнера:
- `youtube-bot-postgres-1` (база данных) - статус `Up`
- `youtube-bot-app-1` (приложение) - статус `Up`

### Шаг 5: Просмотр логов

Если что-то пошло не так, проверьте логи:

```bash
# Логи приложения
docker compose logs app

# Логи базы данных
docker compose logs postgres

# Логи всех сервисов в реальном времени
docker compose logs -f
```

### Шаг 6: Открытие приложения

Откройте в браузере: **http://localhost:8080**

## 🔧 Управление контейнерами

### Остановка контейнеров

```bash
docker compose down
```

### Остановка с удалением данных (⚠️ удалит базу данных!)

```bash
docker compose down -v
```

### Перезапуск контейнеров

```bash
docker compose restart
```

### Пересборка после изменений в коде

```bash
# Остановить контейнеры
docker compose down

# Пересобрать и запустить
docker compose up -d --build
```

### Просмотр использования ресурсов

```bash
docker compose stats
```

## 📊 Полезные команды

### Вход в контейнер приложения

```bash
docker compose exec app sh
```

### Вход в базу данных PostgreSQL

```bash
docker compose exec postgres psql -U postgres -d youtube_market
```

### Выполнение SQL запросов

```bash
docker compose exec postgres psql -U postgres -d youtube_market -c "SELECT COUNT(*) FROM ads;"
```

### Просмотр переменных окружения в контейнере

```bash
docker compose exec app env
```

### Очистка неиспользуемых образов и контейнеров

```bash
# Удалить остановленные контейнеры
docker container prune

# Удалить неиспользуемые образы
docker image prune

# Полная очистка (⚠️ удалит всё неиспользуемое)
docker system prune -a
```

## 🐛 Решение проблем

### Проблема: Порт 8080 уже занят

**Решение:** Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8081:8080"  # Внешний порт:внутренний порт
```

Или остановите процесс, использующий порт 8080.

### Проблема: Контейнер постоянно перезапускается

**Решение:** Проверьте логи:
```bash
docker compose logs app
```

Частые причины:
- Неверный `DATABASE_URL`
- Отсутствует `BOT_TOKEN` или `MANAGER_ID`
- Ошибки в коде

### Проблема: База данных не подключается

**Решение:**
1. Проверьте, что контейнер PostgreSQL запущен: `docker compose ps`
2. Проверьте логи: `docker compose logs postgres`
3. Убедитесь, что в `.env` указан правильный `DATABASE_URL`
4. Попробуйте пересоздать базу данных:
   ```bash
   docker compose down -v
   docker compose up -d
   ```

### Проблема: Изменения в коде не применяются

**Решение:** Пересоберите образ:
```bash
docker compose up -d --build
```

### Проблема: Ошибка "Cannot connect to the Docker daemon"

**Решение:**
- Убедитесь, что Docker Desktop запущен (Windows/Mac)
- Проверьте, что служба Docker запущена (Linux): `sudo systemctl start docker`
- Перезапустите Docker Desktop

### Проблема: Недостаточно места на диске

**Решение:** Очистите неиспользуемые образы:
```bash
docker system prune -a --volumes
```

⚠️ **Внимание:** Это удалит все неиспользуемые образы, контейнеры и volumes!

## 🔄 Обновление проекта

### Обновление кода из Git

```bash
# Остановить контейнеры
docker compose down

# Обновить код
git pull

# Пересобрать и запустить
docker compose up -d --build
```

### Обновление зависимостей

Если изменились зависимости (go.mod, package.json):

```bash
# Остановить контейнеры
docker compose down

# Пересобрать с очисткой кэша
docker compose build --no-cache

# Запустить
docker compose up -d
```

## 📝 Структура Docker-файлов

### Dockerfile

Многоэтапная сборка:
1. **frontend-builder**: Собирает React приложение
2. **backend-builder**: Компилирует Go приложение
3. **Финальный образ**: Минимальный Alpine образ с готовым приложением

### docker-compose.yml

Определяет два сервиса:
- **postgres**: База данных PostgreSQL 16
- **app**: Основное приложение

## 🔐 Безопасность

### Для продакшена:

1. **Измените пароли базы данных** в `docker-compose.yml`:
   ```yaml
   POSTGRES_PASSWORD: ваш_надёжный_пароль
   ```

2. **Используйте секреты** вместо `.env` файла:
   ```bash
   docker secret create bot_token ваш_токен
   ```

3. **Ограничьте доступ к портам** - не открывайте порты наружу без необходимости

4. **Используйте HTTPS** через reverse proxy (nginx, traefik)

## 📚 Дополнительные ресурсы

- [Документация Docker](https://docs.docker.com/)
- [Документация Docker Compose](https://docs.docker.com/compose/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

## ✅ Чеклист перед запуском

- [ ] Docker установлен и запущен
- [ ] Файл `.env` создан с правильными значениями
- [ ] Порт 8080 свободен
- [ ] Достаточно места на диске (минимум 2 ГБ)
- [ ] BOT_TOKEN получен от @BotFather
- [ ] MANAGER_ID получен (можно у @userinfobot)

После выполнения всех пунктов запустите:
```bash
docker compose up -d --build
```

---

**Готово!** 🎉 Ваше приложение должно быть доступно по адресу http://localhost:8080

